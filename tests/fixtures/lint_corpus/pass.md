# Pass

This page is the reference for copy that the linter accepts. Sentences stay short. Each one carries a single idea.

A gate holds a threshold. The engine measures a rate and computes a bound. The bound decides, and the point estimate is shown next to it.

The suite covers four scenario families. Each family draws parameters from a fixed range. Coverage counts how many draws landed in each bin.
