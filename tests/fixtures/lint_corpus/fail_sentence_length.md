# Sentence length

The gate engine reads every result from the suite and evaluates the predicate over each one of them and then it counts the failures and computes a bound and compares that bound against the threshold written in the frozen gate file.
